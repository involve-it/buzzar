// Write your tests here!
// Here is an example.
/*
Tinytest.add('example-main', function (test) {
  debugger;
  test.equal(true, true);
  //test.equal(false, true);
});
*/
describe('sanjo:jasmine on client', function () {
  it('works', function () {
    expect(it).toBeDefined();
  })
})
